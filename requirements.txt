requests>=2.32.0
python-dotenv>=1.0.1
tzdata>=2024.1 ; sys_platform == "win32"

# Optional: only needed if USE_PLAYWRIGHT_FALLBACK=true (see README).
# Install with:  pip install playwright && python -m playwright install chromium
# playwright>=1.62.0
